--- a/node_modules/nx/src/command-line/release/changelog.js
+++ b/node_modules/nx/src/command-line/release/changelog.js
@@ -278,6 +278,7 @@ async function releaseChangelog(args, releaseGroup, options) {
                     return [
                         project,
                         ...(projectsVersionData[project]?.dependentProjects.map((dep) => dep.source) || []),
                     ];
                 })
             : // Otherwise, we use the full list of projects within the release group
                 releaseGroup.projects;
+        console.log(`\n[DEBUG] Release group: ${releaseGroup.name}, projectsRelationship: ${releaseGroup.projectsRelationship}`);
+        console.log(`[DEBUG] Projects array (includes dependents):`, projects);
+        console.log(`[DEBUG] projectsVersionData keys (projects with version changes):`, Object.keys(projectsVersionData));
         const projectNodes = projects.map((name) => projectGraph.nodes[name]);
         if (releaseGroup.projectsRelationship === 'independent') {
             for (const project of projectNodes) {
+                console.log(`\n[DEBUG] Processing project: ${project.name}`);
+                console.log(`[DEBUG] Has version data: ${!!projectsVersionData[project.name]}`);
                 let changes = null;
                 if (releaseGroup.resolvedVersionPlans) {
